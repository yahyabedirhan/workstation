# <project name>

One sentence on what this is and who it is for.

## Setup

Commands to get from clone to running, in order.

## Usage

The one or two commands used daily.

## Agents

Rules live in `AGENTS.md` (`CLAUDE.md` links to it). Skills are pinned in `skills-lock.json`; reinstall with `npx skills@latest add`.
